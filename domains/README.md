# Domains

`domains/` contains literature-anchored models whose assumptions, equations,
and verification path are explicit. Admission here does not mean a model is a
complete description of nature; it means the implementation has a mature,
testable scientific contract.

Each model directory should contain a valid model card and a short guide linking
the card to executable code and tests.
